import { Canvas, Meta, Controls } from '@storybook/blocks';

import * as IconStories from './Icon.stories.svelte';

<Meta of={IconStories} />

# Icon
For displaying icons in the UI, we are using the `Icon` component which extends the 'Iconify' library.

For icons list, visit the [Iconify Icon Set Library](https://icon-sets.iconify.design/).

<Canvas of={IconStories.Default} />
<Controls of={IconStories.Default} />

## Colored Icon
To display colored icons, use the `color` prop.

<Canvas of={IconStories.ColoredIcon} />
