import { Meta, ArgTypes } from '@storybook/blocks';
import { hello } from '@utils';

<Meta title="Utils/General/Hello" />

# Hello Test Function

This is a test function that returns a greeting message. It console logs the message and returns it.

## How to import

```javascript
import { hello } from '@ruzgardogu/utils';
```

## Parameters

<table>
	<thead>
		<tr>
			<th>Name</th>
			<th>Type</th>
			<th>Description</th>
			<th>Default</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td>name</td>
			<td>string</td>
			<td>The name to greet</td>
			<td>"world"</td>
		</tr>
	</tbody>
</table>

## Examples

Here are some usage examples:

```javascript
// Default usage
hello(); // Outputs: "Hello world"

// With custom name
hello('Ruzgar'); // Outputs: "Hello Ruzgar"
```

## Return Value

This function doesn't return a value (void). It only logs the greeting message to the console.

## Type Definition

```typescript
function hello(name?: string): void;
```

## Notes

- If no name is provided, it will use "world" as the default value
- The function will log the greeting message to the console
